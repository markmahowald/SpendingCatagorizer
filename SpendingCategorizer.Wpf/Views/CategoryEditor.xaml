<Window x:Class="SpendingCategorizer.Wpf.Views.CategoryEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpendingCategorizer.Wpf.Views"
        mc:Ignorable="d"
    Title="Add to Existing Category" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Select Category:" />
        <ComboBox Grid.Row="1" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" />

        <TextBlock Grid.Row="2" Text="Description (Refine Match):" />
        <TextBox Grid.Row="3" Text="{Binding FilterString}" />

        <Button  Grid.Row="4" 
                Content="Save" Click ="Button_Click" HorizontalAlignment="Left"/>
    </Grid>
</Window>